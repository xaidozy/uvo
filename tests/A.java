//public class A
//{
    /* try {
            FileReader reader1 = new FileReader(new File("test.txt"));
            BufferedReader reader2 = new BufferedReader(reader1);
            String s = null;
            for (int i = 0; i < 8; i++) {
                s = null;
                try {
                    s = reader2.readLine();
                } catch (IOException e) {
                    e.printStackTrace();
                }
                System.out.println(s + s.length());
            }
        } catch (FileNotFoundException e) {
            e.printStackTrace();
        } */
        /* try {
            streamFis = new FileInputStream(jsFile);
            try {
                streamIsr = new InputStreamReader(streamFis, "GBK");//arg
            } catch (UnsupportedEncodingException e) {
                e.printStackTrace();
            }
        } catch (FileNotFoundException e) {
            e.printStackTrace();
        } */
//}
